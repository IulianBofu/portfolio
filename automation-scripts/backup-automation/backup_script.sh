#!/usr/bin/env bash
# Simple backup script placeholder.
# Replace SRC and DEST with real paths.

SRC="/path/to/source"
DEST="/path/to/backup"

echo "Starting backup from $SRC to $DEST"
# rsync -avh --delete "$SRC" "$DEST"
echo "Backup finished (placeholder)."
